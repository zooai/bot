---
summary: "CLI reference for `zoo-bot clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `zoo-bot clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `zoo-bot clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `zoo-bot clawbot qr` (same behavior as [`zoo-bot qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `zoo-bot clawbot qr` -> `zoo-bot qr`
