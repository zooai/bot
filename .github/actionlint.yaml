# actionlint configuration
# https://github.com/rhysd/actionlint/blob/main/docs/config.md

self-hosted-runner:
  labels:
    # Hanzo self-hosted runners (ARC on hanzo-k8s)
    - hanzo-k8s
    - hanzo-build

# Ignore patterns for known issues
paths:
  .github/workflows/**/*.yml:
    ignore:
      # Ignore shellcheck warnings (we run shellcheck separately)
      - "shellcheck reported issue.+"
      # Ignore intentional if: false for disabled jobs
      - 'constant expression "false" in condition'
      # actionlint's built-in runner label allowlist lags custom runner additions.
      - 'label "hanzo-[^"]+" is unknown\.'
